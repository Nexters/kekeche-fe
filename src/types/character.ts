// TODO: DB 설계되면 수정

export enum Colors {
    RED,
    GREEN,
    BLUE,
    YELLOW,
    ORANGE,
    PINK,
}
export enum Shape {
    CIRCLE,
    RECTANGLE,
    TRIANGLE,
}
export enum Personality {
    ONE,
    TWO,
}
